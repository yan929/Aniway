//Mongo database
MONGO_URI=mongodb://localhost:27017/aniway


//Be careful with this port as macOS uses it for other things, so change the default port 5000 to 5050
//Port for the backend  
PORT=5050
